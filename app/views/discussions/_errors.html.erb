<div id="error-explanation">
    <h2><%= pluralize(object.errors.count, "error") %> prevented this <%= "#{object.class.name.downcase}" %> from being saved </h2>
    <ul>
        <% object.errors.each do |error| %>
            <li><%= error.full_message %></li>
        <% end %>
    </ul>
</div>